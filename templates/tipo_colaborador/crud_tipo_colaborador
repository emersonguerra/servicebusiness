{% extends "base.html" %}
{% block title %}Tipos de Colaborador{% endblock %}
{% block content %}
<h2>Lista de Tipos de Colaborador</h2>
<a href="{{ url_for('tipo_colaborador.add') }}" class="btn btn-success mb-3">Adicionar Tipo</a>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nome</th>
            <th>Descrição</th>
            <th>Data de Criação</th>
            <th>Data de Exclusão</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        {% for tipo in tipos_colaborador %}
        <tr>
            <td>{{ tipo.id }}</td>
            <td>{{ tipo.nome }}</td>
            <td>{{ tipo.descricao }}</td>
            <td>{{ tipo.dtcreation }}</td>
            <td>{{ tipo.dtdeleted }}</td>
            <td>
                <a href="{{ url_for('tipo_colaborador.edit', id=tipo.id) }}" class="btn btn-warning btn-sm">Editar</a>
                <a href="{{ url_for('tipo_colaborador.delete', id=tipo.id) }}" class="btn btn-danger btn-sm" onclick="return confirm('Deseja excluir?')">Excluir</a>
            </td>
        </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}
